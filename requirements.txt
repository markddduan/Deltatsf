# Core DeltaTSF / PatchTST dependencies.
# The reference server environment used torch 2.0.0+cu117. Install the CUDA
# build that matches your system from https://pytorch.org/get-started/locally/.
torch>=1.11.0
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0
scipy>=1.5
matplotlib>=3.4
einops>=0.3
tqdm>=4.62

# Optional: only needed for FLOP profiling via utils/tools.py.
ptflops>=0.6

# Optional: only needed by Pyraformer graph-attention profiling utilities.
pynvml>=11.4
